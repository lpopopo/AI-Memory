# Weekly Summary: 2026-06-08 to 2026-06-14

## Strategy State

The week moved from AI/semiconductor/storage stress into a repair attempt. The most important operational improvement was scope discipline: current portfolio tracking now uses only user/broker-confirmed real-account holdings, while the old USD 20,000 model ledger is retained only for replay/backtest context.

Confirmed real-account equity holdings ended the week at zero after the user-confirmed MRVL sale:

- `MRVL` buy: 1 share at `USD 252.00`.
- `MRVL` sell: 1 share at `USD 267.020`.
- Gross realized result: about `USD +15.020` / `+5.96%`, before fees and FX.

## Market And Risk

- Market fear moved from `stress` toward `elevated / repair watch`, but not to `normal` because VIX/VIX3M term structure remains unavailable and AI/semiconductor/storage beta stayed crowded.
- The strategy kept high cash and no-chase discipline through the rebound.
- AMD reclaimed the historical replay `492` line by the 2026-06-12 completed-session row, allowing replay language to move from `reduce-review` to `repair watch`.
- WDC/STX repaired above `500/835` and remain replay `defensive hold / repair confirmed`.
- MRVL remains `watch only / no chase` after the profitable exit; any re-entry needs a fresh setup.

## Institutional Overlays

The institutional overlay framework became part of daily monitoring but remains experimental:

- `flow_fragility` was elevated to acute during the synchronized AI/semiconductor/storage drawdown, then improved but stayed on medium/elevated watch during the repair.
- `trend_aligned_entry` blocked chase buys and required support/reclaim confirmation.
- `AI_quality/capex_cycle` kept MRVL/AMD/WDC/STX in high capex-cycle sensitivity buckets.
- `factor_macro_flags` repeatedly included growth-duration risk, momentum-reversal risk, theme overlap, sleeve correlation, token-cost elasticity, and VIX term-structure data gaps.
- `bottleneck_watch` continued to cover optical/interconnect, memory/storage, cloud AI factory, and token inference economics.

No overlay rule was promoted to `decisions.md`; the replay sample is still too small.

## Data Workflow

The quote workflow was repaired and revalidated. The current hierarchy is `Tencent -> Yahoo chart -> Sina`, empty arrays are treated as source failures, and the Codex bundled Python path is the only approved Python fallback when Node fails. On 2026-06-14 the required Node smoke test succeeded with structured Tencent quote objects, so Python and Google fallback were not needed for the 23:30 review.

Remaining data gaps:

- reliable VIX and VIX3M close / term structure;
- institutional research checker timed out in the Sunday run;
- real-account cash, fees, FX, tax, settlement, and old broker-side orders remain unconfirmed.

## Next Focus

1. Keep real account cash / no confirmed equity holdings until a fresh setup and user confirmation exist.
2. Use completed close rows only for replay-ledger updates; do not prefill future rows.
3. Confirm whether AMD/WDC/STX repair stays durable or becomes a whipsaw-recovery case.
4. If future exposure is allowed, choose one best confirmed starter rather than rebuilding MRVL/AMD/WDC/STX together.
