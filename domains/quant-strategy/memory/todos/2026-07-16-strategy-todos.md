# 2026-07-16 Strategy Todos

1. 下次人工/券商对账优先核实 QCOM `2` 股是否仍持有；其 2026-07-16 收盘 `170.61 < 182`，状态持续为 `reduce-review`。本记录不是已卖出或已成交的证据。
2. 2026-07-17 完成周线后复核 GLW、MXL、MRVL：GLW 已低于 `166-167` 周线风险带，MXL 已低于 `78`，MRVL 已低于 `223`；按既有长期重分类规则明确各自的 `reduce-review` 与后续处理，但不得因反弹追高。
3. AMD 收盘尚高于 `492`，但距线仅约 1.8%且日内已下穿；后续若完成收盘 `<492`，必须标记 `reduce-review`。WDC/STX 在历史 replay/watch 中已跌破 `500/835`，不得推定为真实持仓或真实成交。
4. 在 `theme_overlap_high`、`sleeve_correlation_high` 与 `trend_broken` 未解除前，关联新买入和摊低保持 **0%**；等待用户/券商提供现金、费用、FX、结算和订单状态后再重算真实 NAV。
