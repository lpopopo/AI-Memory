# 2026-08-13 策略待办

1. 待 8/14 常规盘完成后，以完成周收盘复核 GLW `<166-167` 与 MRVL `<223` 的周度风险带；没有用户/券商确认前，不假设任何订单或成交。
2. 持续跟踪 MXL 的已触发 `<78` completed-close reduce-review，以及 replay/watch 的 AMD `<492`、WDC `<500`；后两者不是实际持仓。
3. 只有 SMH 重回 MA50、相对 QQQ 修复且现有风险复核闭环后，才重新评估 AI-capex sleeve 的新增容量；当前为 `0%`。
