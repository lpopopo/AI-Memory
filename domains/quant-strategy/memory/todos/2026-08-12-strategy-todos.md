# 2026-08-12 策略待办

1. 下次正式审计继续复核 GLW 的 completed-week `<166-167` 带、MRVL 的 `<223` 带与 MXL `<78` 已触发风险；真实账户是否有处置只能以用户或券商回报确认，不推断订单或成交。
2. AMD `482.93 <492`、WDC `454.10 <500` 维持 replay/watch reduce-review；STX `878.21` 虽重回 `835` 上方，仍为 defensive near-stop review。三者均非真实账户持仓。
3. 在 SMH 重回 MA50、相对 QQQ 修复且四项真实持仓风险复核闭环前，维持 AI-capex 相关新增/摊低 `0%`；MRVL 不因事件反弹追高。
4. 下次审计继续获取同步 VIX/VIX3M、广度/信用代理和本地 Node structured quote objects；若可取得期权/流量数据，再细化 flow-fragility，不得把缺失数据当作零风险。
