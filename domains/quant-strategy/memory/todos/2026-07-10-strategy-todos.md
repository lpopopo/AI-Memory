# 2026-07-10 Strategy Todos（收盘审计更新）

## P0：数据与账户事实

1. 以用户/券商回报补齐 DRAM `4 @ 62.20` 的成交时间、卖出费用、FX 与结算；不要把工作估值当成交。
2. 下一正式收盘优先重新抓取同日 VIX3M 与完整日线：本轮 VIX3M `19.00` 为 7 月 9 日最后已验证值，不能继续长期沿用。

## P1：持仓风险与长期规则

3. GLW：只在 completed close `200-205` 上方且回踩守住约 190 后才评估 1 股；若收盘失守 190，停止底部累积并复核；周线 `<166-167` 必须重验长期逻辑。
4. MRVL：4 股已在正常核心规模，禁止加仓；完成收盘稳定 `250-260`、且权重降至约 12% 以下前不评估增持；周线 `<223` 重验。事件反弹不追高。
5. MXL：6 股高于高风险卫星常态，禁止加仓；须权重约 6% 以下、业绩改善且 close `97-100` 上方才重新评估；周线 `<78` 重验。
6. QCOM：持有观察、不加仓；`<185` 升级复核，completed close `<182` 为 failed-entry reduce-review。

## P2：组合与存储方向

7. 在 fear gate 不再为 stress/panic、DRAM 两个 completed close 不破 `61.25` 且第二日 close `>65` 或重获 MA20、SMH 不破约 `566.8`、存储相对强度改善及共同因子容量允许前，不重买 DRAM。
8. 保持有效主题数按一个 AI-capex 共同因子计算；不得以 WDC/STX 单日上涨、或 MRVL/MXL 反弹作为新增仓理由。

## 保留的盘中待办状态

- DRAM 的真实卖出已闭环；继续补齐成交时间、费用、FX 与结算。
- GLW/MRVL/MXL 的未提交 stop-market 建议已被用户长期重分类覆盖；若用户实际已提交任何订单，须由用户/券商确认并自行取消或更新，系统不得推定其存在。
- XLI 已确认 `never placed / no order`；QCOM 仍以 2 股持有观察，禁止加仓。
