# 2026-07-17 Strategy Todos

1. 下次人工/券商对账优先确认 QCOM `2` 股是否仍持有；其正式收盘 `171.78 < 182`，状态持续为 `reduce-review`。本记录不是卖出或成交证据。
2. 7 月 17 日完成周线已使 GLW `<166-167`、MXL `<78`、MRVL `<223` 的既有周线风险复核条件成立。按长线重分类规则逐项形成用户/券商可核对的处理结论；不得把旧未提交的 stop-market 建议误写成真实订单或自动卖出。
3. AMD 收盘 495.76 仍高于 492，但距离仅约 0.76%，且盘中低点 460.21；后续完成收盘 `<492` 必须标记 `reduce-review`。WDC/STX 在 replay/watch 中仍低于 500/835，保持 `reduce-review`，不得推定为真实持仓或成交。
4. 在 `trend_broken`、`theme_overlap_high`、`sleeve_correlation_high` 与风险复核未解除前，关联新买入和摊低保持 **0%**；等用户/券商提供现金、费用、FX、结算和订单状态后再重算真实 NAV。
