# 2026-07-13 Strategy Todos

1. 用户/券商确认 GLW、MXL、MRVL、QCOM 仍为实际持仓，并更新现金、费用、FX 与结算；本快照不替代券商对账。
2. 本周完成后复核：GLW 周收 `<166-167`、MRVL 周收 `<223`、MXL 周收 `<78` 时，分别重新验证长期逻辑与仓位；MRVL 若确认 `<223` 转 `reduce-review`。
3. QCOM 下一完成收盘若 `<182`，升级失败入场 `reduce-review`；在此之前维持 hold/watch、不得加仓。
4. 等待同日有效 VIX3M 以移除期限结构日期错配；在 `elevated 6/14`、flow fragility elevated 和 AI-capex 共同因子集中期间，不新增或摊低任何相关仓位。
