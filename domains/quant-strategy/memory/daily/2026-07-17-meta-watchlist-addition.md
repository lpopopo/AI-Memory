# META 纳入正式自选（2026-07-17）

## 用户指令

- 用户要求将已核验小红书 CXL 笔记中尚未存在于正式自选池的可交易标的加入自选。

## 核对与实施

- 已核对笔记明确提及的可交易美股：`MRVL`、`ALAB`、`MU` 均已在 `references/user-selected-watchlist.json`，不重复添加。
- `META` 为唯一缺失的上市交易标的，已加入正式自选，分类为 `ai_infrastructure_memory_cxl_demand`。
- `Celestial AI` 在笔记中为产业链技术案例，不作为可直接交易的公开股票加入。
- 同步更新 `tools/us_stock_scanner.py` 的 JSON 读取失败备用清单，避免降级扫描遗漏 META。

## 定位与边界

- META 是 AI 基础设施、内存池化/CXL 部署需求的观察级标的，不是本次笔记产生的买入建议、订单、持仓或交易授权。
- 其与 MRVL、ALAB、MU、NVDA 等仍有 AI-capex/数据中心共同因子；加入扫描不降低现有集中度约束，也不创造独立主题容量。
- 后续需用 Meta 原始技术材料、资本开支/服务器部署、供应链收入传导、估值、流动性与相对强度验证。市场 fear gate、完成日线趋势、组合上限和前向证据规则仍优先。

## 前向记录影响

- 既有正式 V9 前向冻结不重写；在新的版本化冻结和预检完成前，META 仅进入研究与全自选扫描，不进入既有前向样本的可执行信号范围。
