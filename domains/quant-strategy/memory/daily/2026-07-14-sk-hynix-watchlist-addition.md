# SK hynix 纳入正式自选（2026-07-14）

## 用户指令

- 用户要求将海力士纳入自选。

## 实施与边界

- 经后续核验，已将初始记录的韩国主上市代码 `000660.KS` 更正为纳斯达克 ADR 代码 `SKHY`（SK hynix ADR），分类为 `memory_storage`，市场标记为 `NASDAQ`。`SKHYV` 是上市初期的临时代码，常规交易代码为 `SKHY`。
- 这是研究与全量扫描范围的扩展，不是买入建议、订单、持仓或交易授权。
- 该标的与 MU、WDC、STX、SNDK、DRAM 同属存储子主题，也与现有 AI-capex / 半导体共同因子高度相关；未来即使出现信号，也须先经过市场风险门、主题上限、跨市场交易可用性、流动性与汇率核对。

## 前向记录影响

- 正式 V9 前向冻结已在 2026-07-12 固定了当时的自选文件哈希，且首个可执行交易日已开始；本次自选变更不得改写或追溯修改该冻结记录。
- 在新的版本化冻结和预检完成前，`SKHY` 仅作为观察标的，不进入既有正式前向样本的可执行信号范围。
