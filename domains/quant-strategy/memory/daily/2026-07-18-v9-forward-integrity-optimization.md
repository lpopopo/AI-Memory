# 2026-07-18 V9 正式前向完整性优化

## 结论

本轮只优化验证与运行可靠性，不调整 V9 正式权重、70 分阈值、Fear Gate、MA150/MA200、仓位、止损或 Rule E 参数。

发现 `scripts/test_forward_integrity.py` 的旧测试准备逻辑会删除正式 `results/shadow_portfolio/forward`，并调用 `freeze_v9_rule_e.py --replace` 重写共享冻结清单。这会使正式前向日状态消失，并造成旧启动审计与当前目录不一致。

## 已完成修改

- 四项前向完整性端到端测试改在独立临时 shadow/frozen 根目录运行，结束后自动清理；不再接触正式 forward/frozen。
- 解除四项测试的安全跳过；验证首日状态、次日执行链、篡改报警与跳日报警。
- 已有任何正式 genesis、日状态、执行、决策、报告或追加事件时，禁止 `freeze_v9_rule_e.py --replace`。
- 正式账户初始化要求 `forward_eligible=true`，并验证基线事件快照哈希；既有 event genesis 不得静默覆盖。
- 新增只读 forward inventory，核对四账户 genesis、逐账户日期集合、完成会话、报告、开盘执行和收盘决策是否一致。
- 预检和启动审计同时输出 manifest identity、日状态文件数、完成会话数、最近完成日期与一致性问题；启动审计也阻断代码哈希不一致。
- 新冻结清单将完整性、事件追加、预检、启动审计、状态库存和对应测试脚本一并纳入代码哈希覆盖。

## 验证结果

- 全套回归：`47 tests`，全部通过，`0 skipped`。
- 当前真实目录审计：四个 genesis、零个日状态、零个完成会话；当前 manifest 为 `forward_eligible=false`。
- 当前数据面只到 `2026-07-14`，不能运行 `2026-07-17` 前向日。
- Rule E 可靠 PIT 事件仍为 `18/50`，资讯源仍为 `partial`。

## 后续边界

先整理并提交当前策略/观察名单/验证变更，再创建新的版本化干净冻结和 genesis；从冻结后第一个真实完成交易日开始连续运行。2026-07-13 至 2026-07-17 的缺口不得回填为前向证据。参数和收益优化继续等待可靠 PIT 样本与新的连续前向证据。
